{
  "name": "test-jee-neet",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@10.5.0",
  "dependencies": {
    "openai": "^6.45.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "overrides": {
    "@types/express": "4.17.25",
    "@types/multer": {
      "@types/express": "4.17.25"
    }
  }
}
