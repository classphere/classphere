/**
 * datalab.routes.ts — REMOVED
 *
 * The Datalab Marker webhook is no longer used.
 * PDF extraction now uses Gemini 2.5 Flash Lite (synchronous, no callbacks).
 */
import { Router } from "express";
const router = Router();
export default router;
